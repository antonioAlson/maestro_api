# Configurações do Servidor
PORT=3000
NODE_ENV=development

# Configurações de CORS
# Em desenvolvimento, use * para permitir todos
# Em produção, especifique o domínio do frontend (ex: https://seu-dominio.com)
CORS_ORIGIN=*

# Configurações do Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=maestro
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui

# JWT Secret (gere uma chave aleatória segura)
JWT_SECRET=sua_chave_secreta_super_segura_aqui
JWT_EXPIRES_IN=7d

# Jira API
JIRA_URL=https://sua-empresa.atlassian.net
JIRA_EMAIL=seu-email@empresa.com
JIRA_API_TOKEN=seu-token-aqui

# Conversao DOCX -> PDF (LibreOffice)
# Windows exemplo: C:\\Program Files\\LibreOffice\\program\\soffice.exe
LIBREOFFICE_PATH=

# Diretório base dos PDFs
PDF_BASE_PATH=C:\\OPs

# IDs dos campos custom do Jira para m² (Espelhos)
# Use GET /api/mirrors/jira-fields?search=metro para descobrir os IDs corretos
# Deixar em branco desabilita a atualização daquele campo
JIRA_FIELD_SQM_8C=
JIRA_FIELD_SQM_9C=
JIRA_FIELD_SQM_11C=
JIRA_FIELD_SQM_TENSYLON=
